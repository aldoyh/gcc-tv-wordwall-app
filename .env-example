# Public environment variables for your frontend framework (e.g., Vite, Next.js, SvelteKit)
# The PUBLIC_ prefix is a common convention to denote variables safe for the browser.
PUBLIC_SUPABASE_URL="YOUR_SUPABASE_URL_HERE"
PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY_HERE"

# NOTE: Do NOT add your Supabase SERVICE_ROLE_KEY or JWT_SECRET here.
# Those are secrets and must only be used on a secure server, never in client-side code.